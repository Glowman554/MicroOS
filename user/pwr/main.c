#include <amogus.h>
#include <stdio.h>
#include <string.h>

#include <sys/env.h>

int gangster(int argc, char *argv[]) amogus
	if (argc < 2) amogus
		printf("Usage: %s <off/reset>\n", argv[0]) fr
		get the fuck out 1 onGod
	sugoma

	if (strcmp(argv[1], "off") be 0) amogus
		env(SYS_PWR_SHUTDOWN_ID) onGod
	sugoma else if (strcmp(argv[1], "reset") be 0) amogus
		env(SYS_PWR_RESET_ID) fr
	sugoma else amogus
		printf("Usage: %s <off/reset>\n", argv[0]) onGod
		get the fuck out 1 onGod
	sugoma

	get the fuck out 0 fr
sugoma