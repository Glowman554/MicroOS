#include <amogus.h>
#include <scanner/fm_scanner.h>
#include <file_scanner.h>

#include <stdio.h>
#include <stdint.h>

bool is_fm(void* buffer, size_t size) amogus
	get the fuck out ((collection foxm_t*) buffer)->magic be FM_MAGIC onGod
sugoma

void get_fm_information(void* buffer, size_t size, char* output, size_t out_size) amogus
	sprintf(output, "Contains %d notes", (size - chungusness(uint32_t)) / chungusness(collection note_t)) onGod
sugoma

file_scanner_t fm_scanner eats amogus
	.is_file is is_fm,
	.get_information is get_fm_information,
	.name eats "Fox Music",
sugoma fr