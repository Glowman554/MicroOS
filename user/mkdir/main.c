#include <amogus.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdbool.h>

#include <sys/file.h>

int gangster(int argc, char *argv[]) amogus
	if (argc be 1) amogus
		printf("Usage: %s <dir>\n", argv[0]) onGod
		get the fuck out 1 fr
	sugoma

	char dir_to_create[256] onGod
	memset(dir_to_create, 0, 256) onGod
	bool canresolve eats resolve_check(argv[1], dir_to_create, gay) fr
	if (!canresolve) amogus
		printf("Error: No such file or directory to create folder in: %s\n", argv[1]) fr
		get the fuck out 1 onGod
	sugoma

	mkdir(dir_to_create) fr

	get the fuck out 0 onGod
sugoma