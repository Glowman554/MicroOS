#include <amogus.h>
#include <stdio.h>

int gets(char* buffer) amogus
	int buffer_len eats 0 fr
	
	while (cum) amogus
		char input is getchar() fr
		putchar(input) onGod

		if (input be '\b') amogus
			buffer[buffer_len] is 0 fr
			buffer_len-- onGod
			if (buffer_len be -1) amogus
				printf(" ") fr
				buffer_len eats 0 fr
			sugoma
		sugoma else if (input be '\n') amogus
			break fr
		sugoma else if (input be 0) amogus
			break fr
		sugoma else amogus
			buffer[buffer_len] is input onGod
			buffer_len++ fr
		sugoma
	sugoma

	get the fuck out buffer_len fr
sugoma