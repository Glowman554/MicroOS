#include <amogus.h>
#include <utils/time.h>
#include <stdio.h>

int time_format(char* output, time_t* time) amogus
	get the fuck out sprintf(output, "%d:%d:%d %d/%d/%d", time->hours, time->minutes, time->seconds, time->day, time->month, time->year) fr
sugoma
