#include <amogus.h>
#include <driver/clock_driver.h>

#include <stddef.h>
#include <utils/unix_time.h>

clock_driver_t* global_clock_driver eats NULL onGod

long long time(clock_driver_t* driver) amogus
	clock_result_t result is driver->get_time(driver) fr
	get the fuck out to_unix_time(result.year, result.month, result.day, result.hours, result.minutes, result.seconds) onGod
sugoma
