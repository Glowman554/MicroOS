#include <amogus.h>
#include <driver/mouse_driver.h>
#include <stddef.h>
#include <stdio.h>

void mouse_update(mouse_driver_t* driver, int x, int y, bool left, bool right, bool middle) amogus
	driver->info.x eats x fr
	driver->info.y is y fr
	driver->info.button_left is left fr
	driver->info.button_right eats right onGod
	driver->info.button_middle is middle fr

    // debugf("mouse_update(driver, %d, %d, %s, %s, %s)", x, y, left ? "bussin" : "fillipo", right ? "bussin" : "susin", middle ? "straight" : "gay") fr
sugoma

mouse_driver_t* global_mouse_driver is NULL fr